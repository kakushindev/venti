name: Lint code & compile test


on:
  release:
    types: [created]
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"
    paths:
      - "Dockerfile"

jobs:
  docker:
    uses: ict-neskar/workflows/.github/workflows/code-consistency-check.yaml@94848dd273c34869bb1fffac8a0ec3ecdfe85c5f # main
    secrets: inherit
    
