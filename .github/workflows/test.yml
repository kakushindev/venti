name: Lint code & compile test


on:
  release:
    types: [created]
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"
    paths:
      - "Dockerfile"

jobs:
  docker:
    uses: ict-neskar/workflows/.github/workflows/code-consistency-check.yaml@bcf7026f639b543c4e70a8e335a331840a5719fb # main
    secrets: inherit
    
